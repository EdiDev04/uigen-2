Your goal is running test and fix the failure tests

Do the following:

1. Run test using `npm test` and indenty the failed test
2. Fix the failed test
3. Run again using `npm test` and make you sure there's not failed test, if you find other errors fix it following the same steps