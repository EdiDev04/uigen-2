Your goal is to update any vulneable dependencies.

Do the following:

1. Run `npm audit` to fin d vulnerable instaled packagesin this project
2. Run `npm audit fix` to apply updates
3. Run tests and verify the updates did not break anything